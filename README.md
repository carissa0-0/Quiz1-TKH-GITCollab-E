# Quiz1-TKH-GITCollab-E

Commit steps (Erics part) creating branches and collab
Need to create new repo, make it public
connect to VS Code,
create new branch called "develop"
require pull requests before merging in main and require approvals from at least 1 collaborator (instructional staff),
Will only follow instructions from Erics part git (which includes all these steps)
make sure to add a commit message to specify,
and push the new changes to remote branch,
merge it with main once all is comeplete.

Goal: is to understand the ways to collaborate and work with team members through methods that won't cause too many complications to the parent or main sheet. 
Independently working with class resources and using VS Code 
