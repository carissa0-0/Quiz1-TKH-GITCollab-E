# Quiz1-TKH-GITCollab-E
(Develop Tab)
HEADER 1 Editing Eric's Project Goal in VS Code. with Markup Style and Templates

BLOCKQUOTE A short description about the project and/or client. - Repo code sheets that test commit knowledge and steps with markdown styles. (include's Eric part in instructions.)

HEADER 2 Getting Started

PARAGRAPH These instructions will get you a copy of the project up and running on your local machine for development and testing purposes. See deployment for notes on how to deploy the project on a live system.

HEADER 3 Prerequisites - needed to have VS Code and Github repo open. 

PARAGRAPH The things you need before installing the software. 

-Need VS CODE
- Need Git and GitHub
- Make sure to have the git bash linked in VS Code 

HEADER 3 Installation

PARAGRAPH A step by step guide that will tell you how to get the development environment up and running.

```
$ Create Repository (public) and ensure you can run the git config command. 
$ Open VS Code and on the terminal, clone the git repo you just created. Make sure to commit a README.md file to the repo.
$ Go to Branch on the repo page, view all branches, and then click on "new branch" and name it Develop.
$ Under Branch name pattern, enter "main"
$ Use git branch and git switch to develop in the VS COde terminal to switch to the develop branch.
$ create and switch to feature-a branch by running git checkout -b feature-a
$ add the two new files based on instruction on doc. Add it in VS code, not in github page.
$ Add file changes to your local repo by running (git add welcome.js getName.js)
$ check status to make sure your files are running
$ Used git commit -m "Message", adding a message of showing a greeting message to user and asking for their name since that is the goal
$ use git push to lead tot he compare and pull request.
$ go back to the github repo page and see if it is in the feature a branch. Once it is all good, you can merge it into the develop branch and then onto the main branch once everything is completed. 
```

HEADER 2 Usage

A few examples of useful commands and/or tasks.

```
$ git branch function
$ git branch
$ git switch develop
$ git status function
$ And use git add function.
$ also, git commit and git pull
```
HEADER 3 Branches

* Main: main, Develop
* Feature: feature a (moved)
* Bugfix: replaced txt commitmsg in main to commitmsgs.
* etc...
